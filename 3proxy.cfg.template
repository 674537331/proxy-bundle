daemon
nserver 1.1.1.1
nserver 8.8.8.8
nscache 65536
timeouts 1 5 30 60 180 1800 15 60

log
auth strong
users ${PROXY_USER}:CL:${PROXY_PASS}

allow ${PROXY_USER}
parent 1000 socks5 ${SS_LOCAL_ADDR} ${SS_LOCAL_PORT}
proxy -n -a -p${HTTP_PROXY_PORT}

flush

auth strong
users ${PROXY_USER}:CL:${PROXY_PASS}
allow ${PROXY_USER}
parent 1000 socks5 ${SS_LOCAL_ADDR} ${SS_LOCAL_PORT}
socks -p${SOCKS_PROXY_PORT}
